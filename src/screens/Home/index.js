import React, { Component } from 'react'
import {
    StyleSheet,
    View,
    Text,
    ScrollView,
    TouchableOpacity,
    SafeAreaView
} from 'react-native';

export default class Home extends Component {
    render() {
        return <SafeAreaView>
            <Text>Anasayfa</Text>
        </SafeAreaView>
    }
}